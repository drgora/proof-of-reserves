# Host-only native codegen: lets the risc0 CPU prover (SHA-NI hashing, AVX-512
# NTT/FRI) use this machine's extensions. Scoped to the x86_64 host target so the
# riscv32im-risc0-zkvm-elf GUEST build is unaffected.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=native"]
